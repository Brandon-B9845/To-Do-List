import React from 'react';
import List from './List';

function Done(props) {
    return (
        <div>
            <h2>Things I have done!</h2>
        <List />

        </div>
    );
}

export default Done;